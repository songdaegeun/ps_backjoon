#include <iostream>
#include <algorithm>
using namespace std;

int ch_set[16];

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
	
	int l, c;
	cin >> l >> c;
	for (int i = 0; i < c; i++)
		cin >> ch_set[i];
	
}

// 4 6
// a t c i s w